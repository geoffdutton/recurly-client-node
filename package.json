{
  "name": "recurly",
  "version": "3.0.0-beta.3",
  "description": "Recurly V3 node client",
  "main": "lib/recurly.js",
  "scripts": {
    "build": "./scripts/build",
    "clean": "rm -f ./lib/recurly/resources/*",
    "coverage": "codecov",
    "lint": "standard --fix 'lib/**/*.js' 'test/**/*.js'",
    "tdd": "mocha --recursive --watch",
    "test": "nyc --reporter=lcov --reporter=text --reporter=html mocha --recursive"
  },
  "author": "Benjamin Eckel <ben@recurly.com>",
  "repository": "https://github.com/recurly/recurly-client-node.git",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "acorn": "^7.0.0",
    "codecov": "^3.5.0",
    "documentation": "^12.1.1",
    "mocha": "^6.2.0",
    "nyc": "^14.1.1",
    "standard": "^14.0.0"
  }
}
